import 'package:flutter/material.dart';

class RemoveEventScreen extends StatelessWidget {
  const RemoveEventScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.pink, child: Text("Remove Event")),
    );
  }
}
