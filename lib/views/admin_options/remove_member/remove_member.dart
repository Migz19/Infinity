import 'package:flutter/material.dart';

class RemoveMemberScreen extends StatelessWidget {
  const RemoveMemberScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.pink, child: Text("Remove Member")),
    );
  }
}
